# S.E.V.E.N. SOLIDARITY
## Mutual Aid Client

Frontend built with Vite + React. Default dev port: 5173.

Set environment var in `.env` (optional):

- `VITE_API_URL` — backend URL (default `http://localhost:4000`).

Run:

```bash
cd client
npm install
npm run dev
```
